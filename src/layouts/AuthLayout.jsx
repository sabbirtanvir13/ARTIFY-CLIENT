import React from 'react';
import Navbar from '../components/Navbar';

const AuthLayout = () => {
    return (
      <nav>
          <Navbar></Navbar>
      </nav>
    );
};

export default AuthLayout;